_schema-version: 3.3.0
ID: WTRL_INC
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: WTRL_INC-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      memory: 100M
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: WTRL_INC-auth
      - name: WTRL_db

  - name: WTRL_db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
      memory: 100M
    requires:
      - name: WTRL_db

resources:
  - name: WTRL_INC-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: WTRL_INC-SOD-dev
        tenant-mode: dedicated
  - name: WTRL_deb
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
